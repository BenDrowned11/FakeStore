<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="FakeStore.Pages.GetSingleProductPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Obtener los Productos"
    BackgroundColor="#F5F5F5">

    <ScrollView>
        <StackLayout Padding="30" Spacing="20" VerticalOptions="CenterAndExpand">

            <Image
                x:Name="ProductImage"
                HeightRequest="250"
                WidthRequest="250"
                Aspect="AspectFill"
                HorizontalOptions="Center"
                Margin="0,20" />

            <Label
                x:Name="ProductTitle"
                FontAttributes="Bold"
                FontSize="28"
                HorizontalOptions="Center"
                TextColor="#34495E" />

            <Label
                x:Name="ProductPrice"
                FontSize="24"
                HorizontalOptions="Center"
                TextColor="Green" />

        </StackLayout>
    </ScrollView>
</ContentPage>
